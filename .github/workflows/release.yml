name: Release APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # R1-38: fail the build if a secret/credential is ever committed.
      - name: Secrets scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Verify dependencies
        run: flutter pub get

      - name: Generate drift code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test

      # R1-1: provision the release keystore from CI secrets. key.properties and
      # key.jks are both gitignored. If secrets are absent, the build proceeds
      # unsigned (never debug-signed) via the Gradle conditional.
      - name: Set up release signing
        id: signing
        run: |
          if [ -n "${{ secrets.ANDROID_KEYSTORE }}" ] && [ -n "${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" ] && [ -n "${{ secrets.ANDROID_KEY_ALIAS }}" ] && [ -n "${{ secrets.ANDROID_KEY_PASSWORD }}" ]; then
            echo "${{ secrets.ANDROID_KEYSTORE }}" | base64 -d > android/key.jks
            printf 'storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}\nkeyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}\nkeyAlias=${{ secrets.ANDROID_KEY_ALIAS }}\nstoreFile=%s/android/key.jks\n' "$GITHUB_WORKSPACE" > android/key.properties
            echo "signing_enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "signing_enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build release APKs (per-ABI split)
        run: flutter build apk --release --split-per-abi --build-number=$GITHUB_RUN_NUMBER

      - name: Build Android App Bundle (AAB)
        run: flutter build appbundle --release --build-number=$GITHUB_RUN_NUMBER

      - name: Upload arm64 APK (recommended for most Android phones)
        uses: actions/upload-artifact@v4
        with:
          name: noteflow-arm64-release-apk
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
          if-no-files-found: warn
          compression-level: 6

      - name: Upload armeabi APK (older 32-bit Android phones)
        uses: actions/upload-artifact@v4
        with:
          name: noteflow-armeabi-release-apk
          path: build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
          if-no-files-found: warn
          compression-level: 6

      - name: Upload AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: noteflow-release-aab
          path: build/app/outputs/bundle/release/app-release.aab
          if-no-files-found: warn
          compression-level: 6